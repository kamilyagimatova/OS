Physical address is real memory location. They are in main memory. User cannot access these addresses.
Virtual address is not real memory location. They generated by CPU while running the application program. They are mapped to the physical address on run time. User can access them.